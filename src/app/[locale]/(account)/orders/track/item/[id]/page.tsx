import TrackingContainer from "@/features/account/orders/tracking-details/TrackingContainer";

const TrackingItem = ({ params }: { params: { id: string } }) => {
  return <TrackingContainer />;
};

export default TrackingItem;
